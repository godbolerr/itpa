template header
age
disabilityPercent
deduction
reason

package com.work.itpa.rules;

template "AT T Rules"
age
disabilityPercent
deduction
reason

rule "Disability_Deduction@{row.rowNumber}"
    when
        FinPerson(age == @{age} , disabilityPercent == @{disabilityPercent}  )
        result : FinPersonResult( )
    then
        result.addMessage("@{reason} " + " @{deduction} ");
end
end template
