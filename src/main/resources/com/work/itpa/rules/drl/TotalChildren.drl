package com.work.itpa.rules;

import java.util.List;
import com.work.itpa.rules.FiConstants;

/**
Messages are global objects 
      fp : FinPerson()
        items : List ( size > 0 ) 
        from collect ( Person ( gender == FiConstants.GENDER_MALE ) from fp.allPersons )

**/

global java.util.List messages

rule "Total Children New"   
    when
    then
        System.out.println(".");
end
